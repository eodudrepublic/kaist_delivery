import 'package:flutter/material.dart';

class AppColors {
  // 연노랑색
  static const Color mainThemeLightColor = Color(0xFFFFF8E7);

  // AppBar, Icon 등에 쓰이는 메인 노랑색
  static const Color mainThemeColor = Color(0xFFFEE6A6);

  // BottomNavigationBar에 쓰이는 진노랑색
  static const Color mainThemeDarkColor = Color(0xFFF1AF23);

  // 검색 위젯에 쓰이는 회색+노랑색
  static const Color searchBackgroundColor = Color(0xFFFFFFFF);

  // 검색 아이콘에 쓰이는 회색
  static const Color searchIconColor = Color(0xFFAEAEAE);

  // 목록에 쓰일 회색
  static const Color listBackgroundColor = Color(0x99CACACA);
}
